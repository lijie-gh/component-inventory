# 元件库存管理 · 改进建议清单

> 这份文档只面向开发（不进源码包、不面向最终用户）。
> 所有「已复现」的条目都用临时库实测过，附复现数据与代码位置，不是猜测。
> 实测环境：`D:\python\python.exe`（3.10.8）· 临时数据库 · 探针脚本跑完即删

---

## 0. 当前状态

**1.0.1（2026-09-19）已修掉第一~第六全部问题**，并顺手接上两处死代码。
修复后的实测数据见每条末尾的「✅ 已修」。

**还没做的**（都不是 bug，是新功能）：

- BOM 核对 + 缺料采购清单（性价比最高，见第四节）
- 盘点表导出 → 填完导回
- 扫码枪连续录入、删除可撤销、自动备份保留策略、库存消耗统计、库位管理界面
- 流水窗口仍只有「全局最近 500 条」和「单个元件」两个视图，缺关键词/日期/类型筛选
- 仍未被界面调用的：`lcsc.suggest()`（按型号搜商城）、`io_utils.parse_any_codes()`；
  `parts.image_url` 抓下来但不显示（需要 Pillow，与「零依赖」冲突，暂时搁置）；
  `stock_market` / `min_purchase` **不在 `PART_FIELDS` 里，根本没落库**，想显示得改表结构

---

## 一、已复现的问题（1.0.1 全部修复）

### 1. ✅ 库存在 3000 件以上时，搜索框边打字边筛会卡住（体感最差）

`ui/main_window.py` 的 `_fill_table()` 每次刷新都把整表删光重插，
3000 次 `treeview.insert` 就要 1.8 秒；而搜索框每敲一个字都走这条路。

| 操作 | 修复前 | 修复后 |
|---|---|---|
| 整表刷新（3000 件） | **1939 ms** | **268 ms** |
| 输入「10kΩ」后刷新 | **1793 ms** | **50 ms** |

**改法**：`MAX_ROWS = 500` —— 表格最多画 500 行（数据一条没少，排序仍在全部结果里进行），
状态栏追加一句「表格最多显示 500 行，请缩小筛选范围」；
删除改成 `tv.delete(*children)` 一次删完；`refresh()` 里重复的 `db.stats()` 去掉一次
（原来 `:393` 与 `:397` 各调一次，每次都要对每个元件解析一遍 JSON 阶梯价）。

### 2. ✅ 刷新后选中行会丢失

`_row_id()` 原来取的是**第一列的值**（C 号），而表格行 id 是数据库主键，
`_fill_table()` 拿它去 `tv.exists(iid)` 永远对不上。

**改法**：`_row_id()` 直接返回 `str(item_id)`（iid 本身就是主键）。
实测：选中第 2 行 → 刷新 → `('2',)` 保持不变。

### 3. ✅ 「参考单价」「库存金额」列点两次箭头变 ↓，数据却还是从小到大

`sort_by()` 对数值列做了 `-float(v)`，外层又 `reverse=self.sort_desc`，
两个取反叠在一起正好抵销。

**改法**：新增 `_NUMERIC_COLUMNS = ("total_qty", "ref_price", "amount")`，
数值列统一「键取负 + 不反转」，文本列仍用 `reverse`。
实测（各点两次）：参考单价 `0.1354 → …`、库存金额 `4.06 → …`、库存 `30 → …` 全部降序，
第三次点击回到升序。

### 4. ✅ 「低库存预警」筛选把零库存的也算进去，和顶部卡片数字对不上

`core/db.py` 的条件是 `qty <= 阈值`，`qty = 0` 也满足；但 `stats()` 把零库存
单独算进「缺货」卡片。卡片 1 个、筛选 2 行 —— 对库存工具是信任问题。

**改法**：筛选条件补上 `COALESCE(s.total_qty,0) > 0`。
实测：筛选 **858 行** = 低库存卡片 **858**，零库存 429 个全部只出现在「零库存」卡片。

### 5. ✅ 「查询商城资料」每次都强制联网，7 天缓存形同虚设

`ui/part_editor.py` 传的是 `fetch_part(code, force=True)`，自动查询也走这条路 ——
`core/lcsc.py` 花力气做的缓存，在最常用的入口被绕过。

**改法**：`_fetch(silent=False, force=True)`，`_auto_fetch()` 传 `force=False`（缓存优先），
手动按钮保持强制联网。
实测：自动查询 `force=False`、手动查询 `force=True`。

### 6. ✅ 没有全局异常兜底，也没有日志文件

全项目 `logging` / `report_callback_exception` / `sys.excepthook` 0 处命中，
而 `main.py` 把 stderr 换成了 `_NullStream` → 打包成无控制台 exe 后，
界面回调里的异常既不报错也不崩，就是「点了没反应」，没有任何可查的东西。

**改法**：新增 `core/applog.py`：

- `report_callback_exception` + `sys.excepthook` 双兜底，写 `data\logs\app.log`
  （512 KB 轮转、留 3 份，`config.LOG_DIR` 与数据库同在 `data\` 下，跟着 exe 走）
- 出错弹提示框（同一次运行最多 3 次，避免连环报错刷屏），弹窗里带上日志路径
- `install()` 返回上一轮错误摘要，`MainWindow._welcome()` 在状态栏提醒一句
- 菜单 **帮助 → 打开运行日志** 直接打开文件

实测：界面回调里抛 `RuntimeError` → 日志里有 ERROR 行 + 弹窗被调用 +
`last_error_summary()` 读得到。

---

## 二、顺手接上的东西（1.0.1）

| 项目 | 说明 |
|---|---|
| 右键 **「查看该元件的出入库记录」** | 原来 `db.list_txns(part_id=…)` 写了但没人调用；现在 `TxnsWindow(part_id=…)` 支持单件视图 |
| 详情栏显示 **整张阶梯价** | 原来 `pricing.describe_tiers()` 是死代码；现在显示 `1+：¥0.135　500+：¥0.105` |
| 批量刷新包进一个事务 | `refresh_all_lcsc()` 原来在循环里一次一提交 |

---

## 三、流水窗口太单薄（未做）

`TxnsWindow`：只有最近 500 条、没有搜索框、没有日期范围、没有类型筛选。
`db.list_txns` 已支持 `part_id`，加 `keyword` / `start_date` / `end_date` 也不难。
仓储场景里流水是要经常查的 —— 这是下一批性价比最高的小改动。

---

## 四、值得做的新功能（按性价比排序，均未做）

### ★★★ BOM 核对与缺料计算 —— 最值得做的一个

现在「批量导入」是把 BOM 往库里加料，但库存工具真正的核心价值是反过来问：

> **「我照这张 BOM 生产，库里够不够？缺什么、缺多少、要花多少钱？」**

零件都齐了：`io_utils.parse_bom_text()` 能解析粘贴的 BOM，
`db.list_parts` / `pricing.price_for_qty` 能算量和价。要做的是：

1. 粘贴 BOM → 逐个匹配库存 → 列三种状态（够 / 缺 / 未收录）；
2. 缺料汇成采购清单：C 号、需求量、现有量、缺口、商城档位单价、小计；
3. 一键导出 CSV，直接拿去买料。

### ★★★ 盘点表导出 → 填完导回

`export_csv` / 导入识别都已有。导出「盘点表」（C号 / 型号 / 库位 / 系统数量 /
**实际数量（空列）**）→ 走一圈仓库 → 导回来自动逐行 `adjust_stock` 并生成盘点流水。
现在盘点只能在窗口里一件一件改。

### ★★ 扫码枪连续录入 / 删除可撤销 / 自动备份保留策略 / 库存消耗统计 / 库位管理

见 2026-09-18 的原始记录（这部分结论没变）。

---

## 五、确认没问题的部分

- **英文界面词条 100% 覆盖**：`tools/extract_ui_strings.py check` 报的「多余」条目
  是多行文本被按行拆开比对造成的，不是漏翻（1.0.1 新增的词条也已被工具识别）。
- **模块级 `T()` 隐患 0 处**：`tools/find_module_level_t.py` 干净。
